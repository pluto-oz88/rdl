(()=>{const q=id=>document.getElementById(id);let points=[],timer=null,index=0,replaySearchPosition=null,lastAnnouncedId=null;const rad=v=>v*Math.PI/180;function kmBetween(a,b){const R=6371,dLat=rad(b.lat-a.lat),dLng=rad(b.lng-a.lng),x=Math.sin(dLat/2)**2+Math.cos(rad(a.lat))*Math.cos(rad(b.lat))*Math.sin(dLng/2)**2;return 2*R*Math.asin(Math.sqrt(x));}function status(t){q('replay-status').textContent=t;}function fmtDuration(s){s=Number(s)||0;if(s<60)return`${s}s`;const m=Math.floor(s/60),r=s%60;return`${m}m ${r}s`;}function speakActivePoi(){if(!q('speak-pois')?.checked)return;const p=state.shortlist[state.activeIndex];if(!p||p.id===lastAnnouncedId)return;lastAnnouncedId=p.id;if(window.roadDiscoverExperience)roadDiscoverExperience.speak(roadDiscoverExperience.announcement(p));}async function refreshJourneys(){try{const r=await fetch(`api/journey.php?_=${Date.now()}`,{cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);const select=q('journey-picker');const current=select.value;select.innerHTML='<option value="">Choose a recorded journey…</option>'+d.journeys.map(j=>`<option value="${j.session}">${j.session} — ${j.count} pts, ${fmtDuration(j.durationSeconds)}</option>`).join('');if([...select.options].some(o=>o.value===current))select.value=current;q('journey-count').textContent=`${d.journeys.length} saved`;status(d.journeys.length?'Choose a journey to load.':'No saved journeys yet.');}catch(e){status(`Could not list journeys: ${e.message}`);}}async function load(){const s=q('journey-picker').value;if(!s){status('Choose a recorded journey first.');return;}status('Loading recorded journey…');try{const r=await fetch(`api/journey.php?session=${encodeURIComponent(s)}&_=${Date.now()}`,{cache:'no-store'}),d=await r.json();if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);points=d.points||[];q('start-replay').disabled=!points.length;status(`${s}: ${points.length} GPS points loaded. Ready to replay.`);}catch(e){points=[];q('start-replay').disabled=true;status(`Could not load recording: ${e.message}`);}}function stop(){if(timer)clearTimeout(timer);timer=null;if(window.roadDiscoverExperience)roadDiscoverExperience.stopSpeech();q('start-replay').disabled=!points.length;q('stop-replay').disabled=true;status(points.length?`Replay stopped at point ${Math.min(index+1,points.length)} of ${points.length}.`:'No recording loaded.');}function feed(p){const c={lat:Number(p.latitude),lng:Number(p.longitude)};q('latitude').value=c.lat.toFixed(6);q('longitude').value=c.lng.toFixed(6);const h=Number(p.heading);if(p.heading!==null&&Number.isFinite(h)){q('heading').value=Math.round(h)%360;if(q('drive-heading-value'))q('drive-heading-value').textContent=`${Math.round(h)%360}°`;}else if(q('drive-heading-value'))q('drive-heading-value').textContent='—';if(q('drive-speed'))q('drive-speed').textContent=p.speed===null?'—':`${(Number(p.speed)*3.6).toFixed(1)} km/h`;if(q('drive-accuracy'))q('drive-accuracy').textContent=p.accuracy===null?'—':`±${Math.round(Number(p.accuracy))} m`;if(q('drive-distance'))q('drive-distance').textContent=replaySearchPosition?`${kmBetween(replaySearchPosition,c).toFixed(2)} km`:'—';}async function step(){if(index>=points.length){timer=null;q('start-replay').disabled=false;q('stop-replay').disabled=true;status(`Replay complete — ${points.length} GPS points.`);return;}const p=points[index];feed(p);status(`Replaying point ${index+1} of ${points.length}.`);if(index===0||index%5===0){await searchGoogle();replaySearchPosition={lat:Number(p.latitude),lng:Number(p.longitude)};if(q('drive-distance'))q('drive-distance').textContent='0.00 km';speakActivePoi();}index++;if(index>=points.length){step();return;}const speed=Math.max(1,Number(q('replay-speed').value)||1),gap=Math.max(100,Math.min(5000,(Number(points[index].timestamp)-Number(p.timestamp))/speed));timer=setTimeout(step,gap);}function start(){if(!points.length||timer)return;index=0;replaySearchPosition=null;lastAnnouncedId=null;q('start-replay').disabled=true;q('stop-replay').disabled=false;step();}q('refresh-journeys').addEventListener('click',refreshJourneys);q('load-replay').addEventListener('click',load);q('start-replay').addEventListener('click',start);q('stop-replay').addEventListener('click',stop);q('journey-picker').addEventListener('change',()=>{points=[];q('start-replay').disabled=true;});refreshJourneys();})();